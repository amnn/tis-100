/* @flow */

import React, { Component } from 'react';
import './TIS.css';

class TIS extends Component {
  render() {
    return (
        <div className={'TIS'}>
        TIS
      </div>
    );
  }
}

export default TIS;
